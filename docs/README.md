In order to read this documentation in the best way possible, you can launch it locally just like this :

# Install dependencies :
```bash
npm i
```

# Run the documentation :
```bash
npm start
```

# You can also build & server the docs :
```bash
npm run build
npm run server
```